  
const priorityInput = document.getElementById('priorityInput');
function getPriority() {
  const selectedPriority = priorityInput.value;
  // Return null if no priority is selected (empty value)
  return selectedPriority || null;
}
  










  
